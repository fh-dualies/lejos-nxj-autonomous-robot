package state;

/**
 * RoboStates is an enumeration that defines the different states of the robot.
 * This enumeration is used to categorize the robot's state and handle it appropriately in the code.
 */
public enum RoboStates {
  IDLE,
  AUTONOMOUS,
  MANUAL,
}
